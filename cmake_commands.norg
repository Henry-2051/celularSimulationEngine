
@code bash
mkdir -p build
cd build

cmake -DCMAKE_BUILD_TYPE=Debug ..

# cmake -DCMAKE_BUILD_TYPE=Release ..

cmake --build .    # this calls make . by default on Unix, or MSBuild/Ninja on other platforms

ctest --output-on-failure

../bin/sfmlgame
@end

* One line build and run   
  ---

@code lang
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release \
  && cmake --build build \
  && ./bin/sfmlgame
@end
